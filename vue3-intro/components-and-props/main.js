const app = Vue.createApp({
   data() {
      return {
         cart: 0,
         premium: true
      }
   },
   methods: {
   },
   computed: {

   }
})

//the cart is not supposed to increment with each button press.